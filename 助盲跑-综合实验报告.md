# 助盲跑 BlindRunner — 项目报告

**小组成员：** 袁纪欢 2023463030735 | 刘绍滨 2023463030720

**Git 仓库：** <https://github.com/lll8488/GuideRun-Android.git>

---

## 一、项目概述

### 1.1 选题背景

视障人群有跑步锻炼的刚性需求，但独立跑步存在严重安全隐患。当前线下专业陪跑采用"陪跑绳引导模式"——志愿者与视障跑者通过约 1 米长的陪跑绳建立物理连接，配合标准化口令完成方向引导、障碍预警与节奏把控。然而，视障跑者"找志愿者难"，志愿者"缺乏专业培训渠道"，双方缺少一个简单、专业、无障碍友好的对接工具。

基于此背景，我们开发了「助盲跑」——一款面向视障跑者与陪跑志愿者的安卓应用，将专业陪跑绳规范转化为应用内的教学、考核与语音辅助体系，实现"发布需求 → 学习考核 → 匹配接单 → 跑步辅助 → 记录回顾"的完整闭环。

### 1.2 产品定位与原则

四个核心原则：**安全**（紧急求助仅拉起拨号盘不自动拨打）、**专业**（内置陪跑绳规范课程与准入考核）、**简单**（大按钮大字号语音导航）、**无障碍**（全场景兼容 TalkBack，视障用户无需视觉辅助即可独立完成所有操作）。

### 1.3 技术选型

| 技术 | 选型 | 理由 |
|------|------|------|
| 语言 | Kotlin | Android 官方推荐，协程支持成熟 |
| 本地存储 | Room (SQLite) | 结构化数据需排序筛选，ORM 减少样板代码 |
| 网络请求 | Retrofit + OkHttp | 注解式 API 定义，成熟的 HTTP 客户端 |
| 异步处理 | Kotlin Coroutines + StateFlow | 官方推荐方案，配合 ViewModel 生命周期 |
| 地图 | 高德地图 SDK 10.0.600 | MapView 成熟，逆地理编码开箱即用 |
| 语音 | Android TextToSpeech | 系统内置，无需第三方 |
| 图表 | GraphView | 轻量折线图库 |
| 架构 | MVVM + Repository | ViewModel 生命周期独立于 Activity，转屏不丢数据 |
| UI | XML 布局 | 高德 MapView 仅支持 XML，团队也更熟悉 |

---

## 二、需求分析

### 2.1 目标用户

**视障用户：** 存在不同程度的视觉障碍，日常通过 TalkBack 操作手机，有跑步锻炼意愿。核心诉求：操作足够简单、信息不依赖视觉、跑步有安全保障。

**陪跑志愿者：** 身体健康、有公益意愿的普通人，对陪跑绳规范不了解但愿意学习。核心诉求：培训流程清晰、考核标准明确、接单信息完整（位置详情与导航）。

### 2.2 核心用户场景

**场景一：视障用户发布陪跑需求。** 用户打开 App，TalkBack 全程朗读，点击"发布需求"。依次选择日期（DatePickerDialog）、时间（TimePickerDialog）、时长（5 档下拉）。地点支持文字输入或地图选点——进入地图界面后自动定位，拖动十字准星，逆地理编码获取地址文字，TalkBack 同步朗读确认。提交前弹窗二次语音确认，确认后需求存库，状态为"待接单"。

**场景二：志愿者学习考核与接单。** 志愿者注册后选择身份，进入陪跑训练营。完成 4 节图文+语音课程（陪跑绳握持方式、方向引导力度控制、转弯避障流程、应急处理），每节支持 TTS 全量播报和进度控制。然后参加 10 道判断题考核，80 分通过后解锁接单权限。浏览待接单需求列表（按时间倒序，支持关键词搜索和标签筛选），点击进入详情页——大字显示信息、静态地图卡片（约 300×200dp）、导航按钮。确认接单后双方联系方式互见，状态更新为"已接单"。

**场景三：跑步语音辅助与紧急求助。** 视障用户点击"开始跑步"，弹窗确认后进入跑步模式：屏幕常亮（FLAG_KEEP_SCREEN_ON），黑色极简界面仅三个按钮，前台 Service 启动（startForeground + START_STICKY），通知栏"助盲跑进行中"常驻。GPS 实时追踪轨迹，每 30 秒 TTS 自动播报标准化口令（"保持平稳呼吸，注意路面情况"等 4 条循环）。跑步中长按音量上键约 3 秒触发紧急求助：震动 500ms → TTS 语音提示 → 拉起系统拨号盘并填入紧急联系人号码（ACTION_DIAL，不自动拨号）。

**场景四：数据记录与回顾。** 跑步结束时确认弹窗，Service 停止，自动生成本次记录（日期、时长、地点、距离、GPS 轨迹 JSON）存入 Room。历史记录页可按日期范围查询，统计卡片实时更新（总次数、总时长、平均时长），长按删除。"统计"页使用 GraphView 绘制折线图，X 轴日期 Y 轴时长。

### 2.3 功能需求总览

**用户体系模块：**
- 手机号 + 验证码注册/登录（MVP 阶段模拟验证码，任意 6 位数字）
- 首次登录强制选择视障/志愿者身份
- 个人信息编辑，按手机号隔离数据（`user_${phone}` 命名 SharedPreferences 文件）
- 视障用户可设置最多 3 位紧急联系人（序列化分隔符存储）
- 个人中心支持身份切换，切换后首页和功能入口随之变化

**陪跑规范学习与考核模块：**
- 4 节陪跑绳规范课程，图文 + TextToSpeech 全量语音播报，支持暂停和进度控制
- 10 道判断题，每题 10 分，80 分及以上通过
- 考核结果双写存储（SharedPreferences + Room UserDao）
- 未通过者接单按钮灰色不可用，提示"请先通过陪跑绳规范考核"

**需求发布与匹配模块：**
- 发布需求字段：跑步日期、时间、地点（文字输入/地图选点）、预计时长（5 档）、模式标签
- 地图选点：全屏地图 + 十字准星 + 逆地理编码 + Battery_Saving 快速定位，确认后地址文字自动回填
- 发布前二次语音确认弹窗
- 需求列表按时间倒序，支持 TextWatcher 关键词搜索和标签距离筛选
- 需求详情：地址大字展示 + 高德静态地图 + ACTION_VIEW 导航
- 接单后双方联系方式互见

**跑步辅助与记录模块：**
- 跑步启停：首页醒目按钮 → 信息确认 → 前台 Service 启动
- 跑步模式：屏幕常亮、极简黑底界面、暂停/结束/求助按钮
- 语音播报：默认 30 秒间隔可调（15/30/60 秒或关闭），4 条预设口令循环播报
- 紧急求助：长按音量上键 3 秒 → 震动 + 语音提示 → 拉起拨号盘（仅填号码不自动拨打）
- 跑步记录自动生成，按日期范围查询，长按删除
- 统计：总次数/总时长/平均时长卡片 + GraphView 折线图

### 2.4 无障碍适配（非功能性需求）

- **TalkBack 兼容：** 所有交互控件设 contentDescription，装饰性元素标记 importantForAccessibility="no"，焦点顺序遵循布局自然序
- **触控标准：** 最小触摸目标 48×48dp，核心按钮 64-80dp，间距 ≥ 8dp
- **视觉补偿：** 高对比度黑白主题选项、sp 单位支持系统大字体、文字背景对比度符合 WCAG 标准
- **语音反馈：** 所有页面进入和操作确认均有 TTS 播报，紧急操作配合震动双重反馈
- **跑步模式：** 屏幕常亮、仅保留核心按钮、全操作语音+震动反馈

### 2.5 MVP 范围

MVP 聚焦上述四个模块。以下不在范围：真实短信验证码、在线支付/押金、即时通讯、社区功能、服务端部署（Ktor 后端代码已写但仅作预留）。

---

## 三、系统设计

### 3.1 整体架构

采用 MVVM + Repository 分层架构：

```
┌─────────────────────────────────────────────┐
│              Presentation Layer              │
│  Activity / Fragment  ←→  ViewModel         │
│       (UI 事件)            (StateFlow)       │
├─────────────────────────────────────────────┤
│              Domain Layer                    │
│  RunningRecord / Demand（纯 Kotlin 数据类）  │
├─────────────────────────────────────────────┤
│              Data Layer                      │
│  RunningRepository（单一数据源）              │
│     ├── Room DAO（本地 SQLite）              │
│     └── Retrofit ApiService（远程 Mock API） │
└─────────────────────────────────────────────┘
```

**为什么选 MVVM：** ViewModel 生命周期比 Activity 长（转屏不丢数据），且不持有 View 引用，避免了 MVP 中 Presenter 持有 View 导致的内存泄漏风险。数据通过 StateFlow 驱动 UI 更新，比 MVP 手动调用 View 方法更简洁。

**当前实现：** MVP 阶段多数页面采用简化方案——Activity 直接通过 `lifecycleScope.launch` 调用 Repository，跳过 ViewModel 层降低代码量。MainActivity 和 BlindHomeActivity 保留完整 ViewModel 绑定作为示范。

### 3.2 数据流与 Repository 策略

```
用户操作 → Activity.onClick()
    ├── ViewModel.loadData() （有 ViewModel 的页面）
    │       └── Repository.getRecords()
    │           ├── Room 本地有数据 → 直接返回 → StateFlow emit
    │           └── Room 为空 → Retrofit 拉取 → 写入 Room → emit
    └── Activity 直接调 Repository （简化方案，流程同上）
```

Repository 核心逻辑：

```kotlin
suspend fun getRecords(forceRefresh: Boolean = false): List<RunningRecord> {
    val localRecords = runningRecordDao.getRecordCount()
    return if (forceRefresh || localRecords == 0) {
        val posts = apiService.getPosts()
        val entities = posts.map { it.toEntity() }
        runningRecordDao.insertAll(entities)
        entities.map { it.toDomainModel() }
    } else {
        runningRecordDao.getAllRecordsRaw().map { it.toDomainModel() }
    }
}
```

### 3.3 页面结构（19 页）

**基础层（3 页）：** SplashActivity（2 秒启动页）→ LoginActivity（手机号+验证码登录，60 秒倒计时）→ IdentitySelectActivity（首次登录选身份）

**视障端（11 页）：**

| 页面 | 核心功能 | 技术要点 |
|------|---------|---------|
| BlindHomeActivity | 首页：三大功能入口 + 我的需求四状态列表 | RecyclerView + ListAdapter + DiffUtil |
| PublishDemandActivity | 发布需求表单 | DatePicker + TimePicker + ActivityResultLauncher |
| MapPickerActivity | 全屏地图+十字准星选点 | AMap + Geocoder + Battery_Saving 定位 |
| StartRunningActivity | 跑步信息确认 | 读取关联需求 |
| RunningModeActivity | 全屏计时+暂停/结束/求助 | Foreground Service + KeyEvent + 屏幕常亮 |
| RunningEndActivity | 结果展示+保存记录+评分 | withTransaction 原子写入 |
| HistoryActivity | 日期筛选+长按删除+统计入口 | SwipeRefreshLayout + DatePickerDialog |
| StatisticsActivity | 统计卡片+折线图 | GraphView LineGraphSeries |
| HistoryDetailActivity | GPS 轨迹回放 | AMap Polyline + LatLngBounds |
| BlindProfileActivity | 信息编辑+紧急联系人+切换身份+退出 | 按手机号隔离 SharedPreferences |
| EmergencyContactActivity | 最多 3 位联系人管理 | 分隔符序列化存储 |
| SettingsActivity | 主题/语音/播报间隔/隐私 | Switch + ThemeHelper |

**志愿者端（7 页）：**

| 页面 | 核心功能 | 技术要点 |
|------|---------|---------|
| VolunteerHomeActivity | 双 RecyclerView（待接单+已完成） | 卡片式入口+状态分区 |
| DemandListActivity | 搜索+6 标签筛选+距离排序 | TextWatcher + Battery_Saving |
| DemandDetailActivity | 大字地址+静态地图+导航+接单 | 高德静态图 API + ACTION_VIEW |
| TrainingCampActivity | 4 节课程入口+考核状态 | 课程状态标记 |
| CourseDetailActivity | 图文内容+TTS 全量播报 | 4 节×约 300 字 |
| ExamActivity | 10 题动态生成 | RadioGroup + AppPrefs + UserDao 双写 |
| VolunteerProfileActivity | 考核状态卡片+信息编辑+切换 | per-user 数据隔离 |
| LeaderboardActivity | 排行榜（排名+次数+评分） | ListAdapter + DiffUtil |

### 3.4 页面流转

```
Splash ──(2s)──→ Login ──(验证通过)──→ ┬── 新用户 → IdentitySelect → BlindHome / VolunteerHome
                                        └── 老用户 → 按 userType 直跳对应首页

视障端核心路径：
  BlindHome
    ├─→ PublishDemand → MapPicker → 确认 → 返回（需求状态：待接单）
    ├─→ StartRunning → RunningMode → RunningEnd → History / Statistics
    └─→ BlindProfile → EmergencyContact / Settings / SwitchIdentity

志愿者端核心路径：
  VolunteerHome
    ├─→ TrainingCamp → CourseDetail(×4) → Exam
    ├─→ DemandList → DemandDetail → 确认接单
    └─→ VolunteerProfile / Leaderboard
```

> 墨刀原型：<https://modao.cc/ai/share/6a00976275d0275ef6df6b2b>

### 3.5 数据库设计

数据库 `blind_runner_db`，两张表：

**running_records：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| date | TEXT | 跑步日期 |
| durationMinutes | INTEGER | 时长（分钟） |
| location | TEXT | 跑步地点 |
| distanceKm | REAL | 距离（公里） |
| status | TEXT | pending / completed / archived |
| ownerPhone | TEXT | 发布者手机号 |
| volunteerPhone | TEXT | 接单志愿者手机号 |
| lat / lng | REAL | 地点经纬度 |
| trackJson | TEXT | GPS 轨迹点 JSON |
| demandId | INTEGER | 关联需求 ID |

**users：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| phone | TEXT UNIQUE | 手机号（唯一标识） |
| name | TEXT | 用户姓名 |
| userType | TEXT | blind / volunteer |
| emergencyContact | TEXT | 紧急联系人电话 |
| examPassed | INTEGER | 考核通过（0/1） |
| examScore | INTEGER | 考核分数 |

实体关系：一个用户可发布多条跑步记录，通过 phone 字段关联。

数据库 Migration 从 1 到 7，逐步加入 ownerPhone、volunteerPhone、blindConfirmed、lat/lng、trackJson、examPassed/examScore 等字段。

### 3.6 双端差异化设计

**视障端：** 纯白背景+深绿主题（#1B5E20）、标题 28sp、按钮 64-80dp、单列布局每屏最多 3-4 操作、所有控件有 contentDescription、跑步模式黑底仅保留三个按钮。

**志愿者端：** 浅灰底色（#F5F6FA）、CardView 卡片+圆角 12-16dp、标题/副标题/元数据三级层级、圆角标签（选中绿/未选灰）、橙色状态徽章。

---

## 四、技术实现

### 4.1 网络层（Retrofit）

ApiService.kt 定义 3 个端点：

| 方法 | 注解 | 用途 |
|------|------|------|
| getPosts() | @GET("posts") | 获取全部数据 |
| getUserById(id) | @GET("users/{id}") | 按 ID 查用户 |
| createPost(request) | @POST("posts") | 提交新数据 |

Mock API 使用 jsonplaceholder.typicode.com，Gson 做 JSON 转换，HttpLoggingInterceptor（BODY 级别）用于调试。RetrofitClient 单例模式管理实例。

### 4.2 数据库层（Room）

两 Entity + 两 Dao + AppDatabase 单例。

RunningRecordDao 提供 CRUD + 按日期范围查询 + 按状态筛选，查询方法返回 Flow 或 suspend 函数，与协程自然集成。UserDao 提供插入、按手机号查询、按身份类型查询。

### 4.3 数据映射

三层模型之间的转换通过扩展函数实现：
- `PostResponse.toEntity()` — API 响应 → Room Entity
- `RunningRecordEntity.toDomainModel()` — Entity → 领域模型
- `RunningRecord.toEntity()` — 领域模型 → Entity

映射函数提取为 Repository 的公开方法，便于单元测试独立验证。

### 4.4 前台 Service（RunningService）

跑步计时和 GPS 追踪的核心。通过 startForeground(1001, notification) 保活，onStartCommand 返回 START_STICKY 保证被杀后自动恢复。GPS 使用 LocationManager.requestLocationUpdates（GPS_PROVIDER，3 秒/5 米），通过 Location.distanceBetween 累计距离。轨迹点存入列表，跑步结束后序列化为 JSON 持久化。

语音播报委托给全局 TtsHelper 单例，避免创建多个 TTS 实例导致音频焦点冲突。通知栏每秒更新显示"已跑步 HH:MM:SS · XX.XX km"。

### 4.5 无障碍 Service（EmergencyAccessibilityService）

继承 AccessibilityService，全局监听音量上键长按。ACTION_DOWN 时启动 3 秒 Handler 计时，超时未松开则触发求助。求助流程：震动 500ms → TTS 语音播报联系人信息 → ACTION_DIAL 拉起拨号盘并填入号码。全程不调用自动拨号 API。fired 标记防止重复触发，2 秒后自动重置。需用户在系统设置中手动开启本服务。

### 4.6 其他关键实现

**TtsHelper：** 全局单例，在 BlindRunnerApp.onCreate() 预初始化减少延迟。AudioAttributes 设为 USAGE_ASSISTANCE_ACCESSIBILITY 避免与 TalkBack 抢焦点。小米设备兼容：引擎初始化失败自动重试最多 3 次。

**身份与数据隔离：** AppPrefs.currentUserPhone 记录当前用户，各页面 SharedPreferences 以 `user_${phone}` 命名，不同用户数据互不干扰。身份切换更新 AppPrefs.userType 并跳转对应首页。

**主题切换：** ThemeHelper.applyTheme() 在 BaseActivity.onCreate() 调用，读取 AppPrefs.highContrastMode 决定主题。因在 super.onCreate() 前调用，首次设置需重启生效。

**地图降级：** 权限被拒绝时地图选点页显示默认城市中心，不影响文字输入地址功能。MapConfig 统一从 Manifest meta-data 读取 API Key，避免硬编码。

---

## 五、测试

### 5.1 单元测试（7 个方法）

**DAO 仪器化测试（4 个）：**

| 测试方法 | 验证点 |
|---------|-------|
| insertAndQueryRecord | 插入后正确查询，字段一致 |
| insertAndDeleteRecord | 插入后删除，再查询为空 |
| queryByStatus_returnsOnlyMatchingRecords | 按状态筛选正确过滤 |
| queryByDateRange_returnsFilteredRecords | 日期范围正确过滤边界外数据 |

**Repository 数据转换测试（3 个）：**

| 测试方法 | 验证点 |
|---------|-------|
| postResponseToEntity_mapsFieldsCorrectly | API 响应各字段正确映射到 Entity |
| entityToDomainModel_preservesAllFields | Entity → Domain 字段值不丢失 |
| domainModelToEntity_preservesAllFields | Domain → Entity 字段值不丢失 |

全部 @Test + runTest 编写，编译通过。

### 5.2 集成测试（15 条）

**核心场景（10 条）：**

| 编号 | 场景 | 关键步骤 | 结果 |
|------|------|---------|:---:|
| TC-01 | 启动→登录→身份选择 | 手机号+验证码→登录→选视障用户 | ✅ |
| TC-02 | 视障发布需求 | 选日期/时间/地点/时长→发布 | ✅ |
| TC-03 | 志愿者考核→接单 | 课程→考核（全对）→需求列表→接单 | ✅ |
| TC-04 | 跑步→结束→查记录 | 开始跑步→暂停→继续→结束→历史 | ✅ |
| TC-05 | 历史记录筛选删除 | 选日期范围→查统计→长按删除 | ✅ |
| TC-06 | 紧急联系人设置 | 添加 2 位联系人→保存→验证回显 | ✅ |
| TC-07 | 辅助功能设置 | 切换高对比度/语音/播报间隔 | ✅ |
| TC-08 | 身份切换 | 视障→志愿者→进入志愿者首页 | ✅ |
| TC-09 | 地图选点 | 定位→确认位置→地址回传 | ⚠️ |
| TC-10 | 跑步紧急求助 | 旋转屏幕→求助按钮 | ❌ |

**边界与异常（5 条）：**

| 编号 | 场景 | 结果 |
|------|------|:---:|
| TC-11 | 手机号位数校验（少于 11 位） | ✅ |
| TC-12 | 发布需求必填校验（缺日期） | ✅ |
| TC-13 | 空验证码登录 | ✅ |
| TC-14 | 考核未通过点接单 | ✅ |
| TC-15 | 历史记录空数据 | ✅ |

通过率 87%（13/15）。TC-09 无权限时定位降级（属于设计降级策略）；TC-10 旋转重置（需 onSaveInstanceState）。

### 5.3 Bug 修复情况

累计 20 个 Bug。P0 修复率 80%（4/5，地图选点回传、搜索筛选无功能、保存不持久化、UI 线程崩溃已修复；Service 计时竞态未修复需重构）。P1 修复率 60%（3/5，紧急联系人只存 1 位、验证码无倒计时、语音播报空壳已修复）。P2/P3 修复 3/8，剩余主要为课程范围外问题（模拟验证码、GraphView 兼容性、DiffUtil 优化等）。

### 5.4 性能评估

| 场景 | 内存 |
|------|------|
| 启动后 | ~18MB |
| 视障首页 | ~24MB |
| 需求列表（100条） | ~32MB |
| 跑步模式运行中 | ~38MB |
| 连续切换 10 页 | ~45MB（无泄漏） |

冷启动约 3s（< 5s 标准），测试全程无崩溃。

---

*报告完成日期：2026-07-08*
