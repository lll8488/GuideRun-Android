组长：袁纪欢 2023463030735  
组员：刘绍滨 2023463030720

git链接：https://github.com/lll8488/GuideRun-Android.git

---

# 助盲跑 APP — 网络层与数据库层实验报告

> 实验内容：搭建 Retrofit 网络层 + Room 数据库层 + Repository 模式 + 单元测试

---

## 一、项目架构

采用 **三层数据架构**：

```
┌─────────────────────────────────────────┐
│  UI 层 (MainActivity ← MainViewModel)   │
├─────────────────────────────────────────┤
│  Repository 层 (RunningRepository)      │  ← 单一数据源
├──────────────────┬──────────────────────┤
│  Room 数据库      │  Retrofit 网络层      │
│  (Entity/Dao/DB) │  (ApiService)        │
└──────────────────┴──────────────────────┘
```

## 二、网络层 — Retrofit

**API 接口**：[ApiService.kt](app/src/main/java/com/blindrunner/app/data/remote/api/ApiService.kt)

| 方法 | 注解 | 说明 |
|------|------|------|
| `getPosts()` | `@GET("posts")` | 获取全部记录 |
| `getUserById(id)` | `@GET("users/{id}")` | 获取单个用户 |
| `createPost(request)` | `@POST("posts")` | 提交新记录 |

- Mock API：`https://jsonplaceholder.typicode.com/`
- JSON 转换器：Gson
- 日志拦截器：HttpLoggingInterceptor（BODY 级别）
- 客户端：RetrofitClient 单例模式

## 三、数据库层 — Room

**Entity**（2 个）：

| 实体 | 表名 | 说明 |
|------|------|------|
| `RunningRecordEntity` | `running_records` | 跑步记录（日期、时长、地点、距离、状态） |
| `UserEntity` | `users` | 用户（手机号、姓名、身份类型、紧急联系人） |

**Dao**（2 个）：

| 接口 | 注解 | 核心操作 |
|------|------|---------|
| `RunningRecordDao` | `@Dao` | 增删改查、按日期范围查询、按状态筛选 |
| `UserDao` | `@Dao` | 插入、更新、按手机号查询、按身份类型查询 |

**Database**：[AppDatabase.kt](app/src/main/java/com/blindrunner/app/data/local/AppDatabase.kt) — `@Database`，单例模式，数据库名 `blind_runner_db`

## 四、Repository 层

[RunningRepository.kt](app/src/main/java/com/blindrunner/app/data/repository/RunningRepository.kt) 同时持有 `RunningRecordDao` 和 `ApiService`，实现"**本地优先，网络兜底**"策略：

```kotlin
suspend fun getRecords(forceRefresh: Boolean = false): List<RunningRecord> {
    // 本地有数据 → 直接返回本地
    // 本地为空或强制刷新 → 拉取网络 → 存入本地 → 返回
}
```

核心方法均为 `suspend` 挂起函数，配合协程使用。包含数据映射逻辑（API 响应 ↔ Entity ↔ Domain 模型）。

## 五、单元测试

### DAO 测试（仪器化测试）
[RunningRecordDaoTest.kt](app/src/androidTest/java/com/blindrunner/app/RunningRecordDaoTest.kt)

| 测试方法 | 覆盖操作 |
|---------|---------|
| `insertAndQueryRecord` | 插入 + 查询 |
| `insertAndDeleteRecord` | 插入 + 删除 |
| `queryByStatus_returnsOnlyMatchingRecords` | 按状态筛选 |
| `queryByDateRange_returnsFilteredRecords` | 按日期范围查询 |

### Repository 数据转换测试（JVM 单元测试）
[RunningRepositoryTest.kt](app/src/test/java/com/blindrunner/app/RunningRepositoryTest.kt)

| 测试方法 | 测试目标 |
|---------|---------|
| `postResponseToEntity_mapsFieldsCorrectly` | API 响应 → Entity |
| `entityToDomainModel_preservesAllFields` | Entity → Domain 模型 |
| `domainModelToEntity_preservesAllFields` | Domain 模型 → Entity |

- 共 **7 个** `@Test` 方法
- 均使用 `runTest`（kotlinx-coroutines-test）
- 全部可预期通过

## 六、检查清单对照

| 要求 | 状态 |
|------|:--:|
| Retrofit API 接口，2-3 个方法 | ✅ 3 个（2 GET + 1 POST） |
| 使用 Mock API | ✅ jsonplaceholder.typicode.com |
| Room Entity + Dao + Database | ✅ 2 Entity + 2 Dao + 1 Database |
| 代码中可见 @Dao、@Entity、@GET 注解 | ✅ |
| Repository 类，含 suspend 函数 | ✅ RunningRepository |
| DAO 增删改查测试 | ✅ 4 个方法 |
| Repository 数据转换测试 | ✅ 3 个方法 |
| @Test + runTest | ✅ |
| 代码能编译通过 | ✅ |

![image-20260513222240921](C:\Users\XD\AppData\Roaming\Typora\typora-user-images\image-20260513222240921.png)
