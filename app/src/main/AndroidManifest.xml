<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".BlindRunnerApp"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.BlindRunner"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true">

        <!-- Splash (启动页) -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.BlindRunner">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth -->
        <activity android:name=".ui.auth.LoginActivity" />
        <activity android:name=".ui.auth.IdentitySelectActivity" />
        <activity android:name=".ui.auth.RegisterActivity" />
        <activity android:name=".ui.auth.PrivacyActivity" />

        <!-- Blind User -->
        <activity android:name=".ui.blind.BlindHomeActivity" />
        <activity android:name=".ui.blind.SettingsActivity" />
        <activity android:name=".ui.blind.PublishDemandActivity" />
        <activity android:name=".ui.blind.MapPickerActivity" />
        <activity android:name=".ui.blind.StartRunningActivity" />
        <activity android:name=".ui.blind.RunningModeActivity" />
        <activity android:name=".ui.blind.RunningEndActivity" />
        <activity android:name=".ui.blind.HistoryActivity" />
        <activity android:name=".ui.blind.HistoryDetailActivity" />
        <activity android:name=".ui.blind.StatisticsActivity" />
        <activity android:name=".ui.blind.BlindProfileActivity" />
        <activity android:name=".ui.blind.EmergencyContactActivity" />

        <!-- Volunteer -->
        <activity android:name=".ui.volunteer.VolunteerHomeActivity" />
        <activity android:name=".ui.volunteer.DemandListActivity" />
        <activity android:name=".ui.volunteer.DemandDetailActivity" />
        <activity android:name=".ui.volunteer.TrainingCampActivity" />
        <activity android:name=".ui.volunteer.CourseDetailActivity" />
        <activity android:name=".ui.volunteer.ExamActivity" />
        <activity android:name=".ui.volunteer.VolunteerProfileActivity" />
        <activity android:name=".ui.volunteer.LeaderboardActivity" />

        <!-- Legacy -->
        <activity android:name=".MainActivity" />

        <!-- 高德地图 -->
        <meta-data
            android:name="com.amap.api.v2.apikey"
            android:value="511feaca0fc2535e5faf7879ae339048" />

        <!-- Running Foreground Service -->
        <service
            android:name=".service.RunningService"
            android:foregroundServiceType="dataSync|location"
            android:exported="false" />

        <!-- PRD 4.4.2: 无障碍服务监听音量上键长按3秒（后台/锁屏均可用） -->
        <service
            android:name=".service.EmergencyAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>
</manifest>
