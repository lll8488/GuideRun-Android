# 助盲跑 APP — 企业级审查报告

> 以生产环境上市标准逐项审查，列出所有功能缺口、不完整页面、逻辑缺陷

---

## 🔴 P0 — 致命缺陷（功能完全不可用或数据丢失）

### 1. 地图选点结果无法回传
`PublishDemandActivity` 启动 `MapPickerActivity` 用的是 `startActivity()` 而非 `startActivityForResult()` / `registerForActivityResult()`。MapPicker 端确实调了 `setResult(RESULT_OK, intent)`，但发布页**没有任何 `onActivityResult` 接收逻辑**。用户在 MapPicker 选择的地址永远无法填入发布页的地址输入框。

### 2. 需求列表搜索/筛选是假功能
`DemandListActivity` 的搜索输入框（`et_search`）、搜索按钮、筛选标签（全部/30分/45分/1小时）只有 UI 样式，**无任何点击监听或过滤逻辑**。标签连 `setOnClickListener` 都没绑定。

### 3. 志愿者端考核状态指示缺失
`DemandListActivity` 布局中 `tv_exam_status` 默认 `visibility=gone`，代码中从未根据实际 `exam_passed` 状态显示/隐藏。**未通过考核的志愿者看不到任何提示**，可以进入列表，只有点了接单才知道不行——不符合 PRD "接单按钮灰色不可点击" 的要求。同样 `TrainingCampActivity` 的 `tv_exam_status` 也未绑定数据。

### 4. 多个页面的"保存"按钮是假的
`BlindProfileActivity` 和 `VolunteerProfileActivity` 的"保存"按钮只弹出 Toast "保存成功"，**姓名和联系方式没有写入 Room 或 SharedPreferences**，下次打开信息消失。

---

## 🟠 P1 — 严重缺陷（核心体验断裂）

### 5. 无任何 loading 状态
全 App 0 个 `ProgressBar`。所有异步操作（Room 查询、网络请求）期间用户看到的是**空白页面或旧数据闪烁**，无任何加载指示。

### 6. 无空状态设计
5 个 RecyclerView 列表（历史记录/需求列表/视障首页需求/志愿者首页接单）在无数据时都是**纯白空白区**，没有"暂无记录"、"去发布第一个需求吧"等引导。

### 7. 错误处理大面积空白或静默吞噬
以下位置 catch 块内为空，错误被完全静默：`BlindHomeActivity.loadMyDemands()`、`VolunteerHomeActivity` 数据加载、`MapPickerActivity` 定位、`StartRunningActivity` 查询。出现异常时**用户不知情，页面保持旧状态**。

### 8. 跑步暂停不计入总时长
`RunningModeActivity` 暂停时计时器暂停，但 `seconds` 继续累加。实际上暂停期间的秒数被算进了总时长。`RunningService.incrementSecond()` 有 `if (!isPaused)` 保护，但 `RunningModeActivity` 的 `timerRunnable` 也独立累加，两套计时逻辑不同步。

### 9. 无页面间数据刷新机制
视障发布需求后返回首页，首页的"我的需求"列表只在 `onResume` 刷新，但 `PublishDemandActivity` 用了 `finish()` 才返回，此前数据已插入 Room。**实际测试发现 onResume 中的刷新能工作**——但志愿者首页的"我的接单"列表只在 `onCreate` 加载一次，接单后返回首页**不会自动刷新**。

---

## 🟡 P2 — 重要缺陷（功能不完整或体验差）

### 10. 导航到此处是空壳
`DemandDetailActivity` 的"导航到此处"按钮只弹 Toast。应调用 `Intent.ACTION_VIEW` 打开系统地图（Google Maps / 高德）。

### 11. 课程详情语音播报未接入
`CourseDetailActivity` 的"语音播报"按钮只弹 Toast "开始语音播报..."，**没有调用 TtsHelper.speak()**。

### 12. 登录页验证码无倒计时
"获取验证码"按钮点击后可无限点击，无 60 秒冷却。不符合任何主流 App 的验证码体验标准。

### 13. 身份选择页无返回按钮
`IdentitySelectActivity` 没有返回按钮。用户如果误入此页，无法退回登录页重新输入手机号。

### 14. 个人中心信息不持久化
姓名/联系方式只存 Toast，下次打开为空。紧急联系人存了 SharedPrefs 但回显时输入框仍为空。

### 15. 数据不互通
用户发布了需求（Room 写入 `status=pending`），志愿者接单后写入新记录（`status=accepted`）。**两条记录是独立的两行**，没有通过任何外键或 ID 关联。视障用户的"我的需求"列表和志愿者的"已接单"列表本质上是不同的数据行。

### 16. 高对比度主题切换需重启
`SettingsActivity` 切换高对比度后只存了 SharedPrefs + Toast。`ThemeHelper.applyTheme()` 在 `onCreate` 前调用才生效，但各 Activity 的 `onCreate` 在 `super.onCreate` 前没有调用 `ThemeHelper`。

---

## 🔵 P3 — 体验缺陷（不影响功能但有损品质）

### 17. 无 SwipeRefreshLayout
所有列表都不支持下拉刷新。用户必须离开页面再回来才能看到新数据。

### 18. 无确认弹窗（除历史删除外）
发布需求、接单、结束跑步等重要操作缺少二次确认。发布后直接 finish，无法撤销。

### 19. 硬编码中文，无国际化
所有 UI 文字硬编码在布局 XML 或 Kotlin 源码中。连 `strings.xml` 都没用全。无法做多语言。

### 20. 无横屏适配
所有布局仅竖屏可用，横屏会严重变形或按钮溢出。

### 21. 无 Crash 收集
没有接入 Firebase Crashlytics、Bugly 等崩溃收集 SDK。生产环境下出现崩溃完全不知情。

### 22. 无网络状态检测
没有 `ConnectivityManager` 检查。网络断开时 Repository 报错被 catch 吞噬，用户不知道是网络问题。

### 23. 无隐私协议/用户协议
无任何合规页面（隐私政策、用户协议）。上架应用商店会被拒。

### 24. 登录/注册逻辑脆弱
"验证码"随便填就过。没有 Token 机制、没有 Session 管理、没有登出功能。

### 25. 无依赖注入框架
`BlindRunnerApp` 用 `lateinit var` 手动管理依赖。超过 10 个 Activity 都通过 `(application as BlindRunnerApp).repository` 获取 Repository。没有 Hilt/Koin，单元测试困难，耦合严重。

### 26. MyOrdersAdapter 无点击事件
志愿者首页的"我的接单"列表只能看，不能点。应该点进去看需求详情或联系对方。

### 27. 无通知推送
需求被接单后，视障用户没有任何推送通知（App 内或系统级）。PRD 要求的"通知栏常驻通知"仅在跑步时实现，业务通知为零。

### 28. DemandAdapter 的 onBindViewHolder 每次重新 findViewById
Adapter 的 ViewHolder 使用了 `view.findViewById`，虽然只在创建时调用一次，但未使用 ViewBinding。对列表性能影响小，但不符合 Google 推荐的最佳实践。

### 29. 无深色模式
虽然做了高对比度主题，但**没有跟随系统的 Dark Mode**，也没有 App 内独立的深色主题。

---

## 功能完成度矩阵

| 模块 | 已完成 | 未完成/有缺陷 |
|------|:--:|------|
| 启动/登录/身份选择 | 80% | 登录无 Token/Session，验证码无倒计时，身份选择无返回 |
| 发布需求 | 70% | 地图选点结果不回传，无二次确认，无法撤销 |
| 地图选点 | 60% | 定位功能有但结果回传断裂，无真实地图控件 |
| 跑步模式 | 85% | 暂停计时逻辑不同步，无前台 Service 电量优化 |
| 跑步结束/保存 | 75% | 地点来自 SharedPrefs 而非可靠来源 |
| 历史记录 | 85% | 长按删除可用，统计弹窗可用，无图表 |
| 需求列表/详情 | 60% | 搜索筛选假功能，导航假功能，状态指示缺失 |
| 训练营/考核 | 80% | 课程语音未接入，训练营状态未绑定数据 |
| 个人中心 | 50% | 保存假功能，信息不回显，紧急联系人只存不读 |
| 设置页 | 90% | 高对比度生效需重启 |
| 无障碍 | 70% | TalkBack 标签全覆盖，但跳转语音不完整 |
| 数据层 | 85% | Room CRUD 完整，Repository 策略完整 |

---

## 建议修复优先级（最少要修到能上线的）

```
第一轮（P0）：
  1. 地图选点回传
  2. 需求列表搜索筛选逻辑
  3. 志愿者端考核状态指示
  4. 个人中心保存持久化

第二轮（P1）：
  5. 全列表 loading + empty 状态
  6. 错误处理不静默
  7. 跑步计时逻辑同步
  8. 页面数据刷新机制

第三轮（P2）：
  9. 导航跳转系统地图
  10. 课程语音接入
  11. 验证码倒计时
  12. 身份选择加返回

第四轮（P3）：
  13. SwipeRefreshLayout
  14. 确认弹窗
  15. 国际化
  16. Crash 收集
```
